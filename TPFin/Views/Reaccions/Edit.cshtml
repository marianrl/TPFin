@model TPFin.Models.Reaccion

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Reaccion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="tipoReaccion" class="control-label"></label>
                <input asp-for="tipoReaccion" class="form-control" />
                <span asp-validation-for="tipoReaccion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="idPost" class="control-label"></label>
                <select asp-for="idPost" class="form-control" asp-items="ViewBag.idPost"></select>
                <span asp-validation-for="idPost" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="idUser" class="control-label"></label>
                <select asp-for="idUser" class="form-control" asp-items="ViewBag.idUser"></select>
                <span asp-validation-for="idUser" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
